# Carga automáticamente los dashboards JSON de la carpeta al arrancar Grafana.
apiVersion: 1

providers:
  - name: "Taller de Diego - SRE"
    orgId: 1
    folder: "SRE"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
